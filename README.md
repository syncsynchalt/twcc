# twcc

The Worst C Compiler

## Why

I wanted to learn arm asm, so why not write a self-hosted C compiler?

## What

A self-hosted compiler is one that can compile itself, producing a working compiler (that can compile itself, producing a working compiler).

You can see how interesting this is, ontologically.
